import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.plantsky',
  appName: 'Plantsky',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
